<onekit-select Style="width:100px;height:30px;" bindchange="changeOption">
          <onekit-option a:for="{{items}}" a:key="key" value="{{item.key}}">{{item.name}}</onekit-option>
        </onekit-select>